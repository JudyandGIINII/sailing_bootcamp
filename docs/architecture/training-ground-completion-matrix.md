# Training Ground completion matrix — synthetic prototype evidence

## Scope and status vocabulary

This is a repository-evidence matrix for the personal, browser-local Training Ground. It records what the current prototype exposes or records; it is not an acceptance of a lesson’s semantic content.

- `IMPLEMENTED_PROTOTYPE` — present in the local prototype and covered by the cited source/test evidence.
- `DECLARED_SYNTHETIC_ONLY` — a manifest/UI declaration is intentionally synthetic and unvalidated; it is not a calculated real-world value.
- `NOT_IMPLEMENTED` — no implementation supplies the described physical, navigational, safety, or correctness behavior.
- `NOT_APPLICABLE` — the facet does not apply to the lesson. No facet below is marked this way because all six are part of each lesson surface.

All lessons use `training-sloop-v1`, registered lesson action policy, and the shared synthetic scenario/replay shell. The visible boundary is **“Simulation-only prototype • Unvalidated content • Not navigation, safety, or certification guidance.”** (`tests/smoke/app.spec.ts`, `runs the keyboard-only L01 prototype with visible non-navigation status`).

| Lesson | Briefing / visible synthetic non-navigation boundary | Required observations | Allowed controls and disallowed controls | Immutable ledger checkpoint behavior | Debrief / causal record behavior | Reset / local replay / fail-closed compatibility |
|---|---|---|---|---|---|---|
| L01 | `IMPLEMENTED_PROTOTYPE` — shared start screen/session heading and visible boundary; the start freezes a synthetic Replay V2 snapshot. Evidence: `tests/smoke/app.spec.ts`, `keeps a labelled synthetic draft editable, then freezes and resets its V2 session`; `runs the keyboard-only L01 prototype with visible non-navigation status`. | `IMPLEMENTED_PROTOTYPE` — manifest-only HUD projects four fields, each `declared_unavailable`: true wind, apparent wind, heading, COG. No value is calculated. Evidence: `src/content/l01.ts` (`required_observations`); `src/content/lesson-manifest.ts` (`projectLessonObservations`); `tests/contracts/lesson-manifest.test.ts`, `projects every observation...`. `NOT_IMPLEMENTED` — real wind/course values or physics. | `IMPLEMENTED_PROTOTYPE` — `helm_port`, `helm_starboard`, `pause`, `resume`, `reset`; unregistered actions deny without state change. Evidence: `src/content/l01.ts`; `src/content/lesson-manifest.ts`; `tests/unit/sim-session.test.ts`, `rejects a direct canonical action outside the lesson policy...`; `tests/smoke/app.spec.ts`, `ignores globally mapped keyboard actions...`. | `IMPLEMENTED_PROTOTYPE` — accepted helm port/starboard appends frozen `LESSON_CHECKPOINT` with `lesson_id: L01` and cause `declared helm correction recorded`; heading/COG remain unavailable and no safety result is made. Evidence: `src/sim/session.ts` (`applyCanonicalInput`); `tests/unit/sim-session.test.ts`, `records the declared L01 helm-correction checkpoint immutably...`. | `IMPLEMENTED_PROTOTYPE` — generic pure debrief projects action/checkpoint records; score is zero-point `draft_causal_checkpoint_recorded` when a checkpoint exists. Evidence: `src/scoring/projection.ts` (`projectScore`, `projectDebrief`); `tests/unit/sim-session.test.ts`, L01 checkpoint regression. `NOT_IMPLEMENTED` — course, safety, or correctness outcome. | `IMPLEMENTED_PROTOTYPE` — reset restores declared initial state; local attempts persist independently and can be deleted; V2 identity/snapshot validation rejects malformed, incomplete, unregistered, or incompatible payloads while preserving them. Evidence: `src/sim/session.ts`; `src/storage/replays.ts`; `tests/smoke/app.spec.ts`, `keeps reset attempts locally...`; `tests/unit/storage-replays.test.ts`; `tests/contracts/replay.test.ts`. |
| L02 | `IMPLEMENTED_PROTOTYPE` — same shared synthetic/non-navigation session boundary. Evidence: `tests/smoke/app.spec.ts`, `projects L02 through L05...`. | `IMPLEMENTED_PROTOTYPE` — HUD projects status-tagged fields. `DECLARED_SYNTHETIC_ONLY` — declared trim feedback. `NOT_IMPLEMENTED` — apparent-wind angle, main/jib sheet values, speed response, and control stability remain `declared_unavailable`. Evidence: `src/content/l02-l05.ts` (`l02Manifest`); `tests/smoke/app.spec.ts`, L02 observation assertion. | `IMPLEMENTED_PROTOTYPE` — helm, `main_trim`, `jib_trim`, pause/resume/reset only; other registered-global actions remain denied. Evidence: `src/content/l02-l05.ts`; `tests/contracts/lesson-manifest.test.ts`; `tests/smoke/app.spec.ts`, `ignores globally mapped keyboard actions...`. | `IMPLEMENTED_PROTOTYPE` — after both declared trim actions, the immutable ledger records L02 checkpoint cause `main/jib synthetic trim causality recorded`. Evidence: `src/sim/session.ts`; `tests/unit/l02-l05.test.ts`; `tests/unit/projection.test.ts`, L02 trace tests. | `IMPLEMENTED_PROTOTYPE` — generic debrief plus separate display-only L02 action/checkpoint record projection. It does not infer performance or advice. Evidence: `src/scoring/projection.ts` (`projectL02RuntimeTrace`); `tests/smoke/app.spec.ts`, `renders L02 browser-local recorded evidence...`. | `IMPLEMENTED_PROTOTYPE` — shared reset/local attempt/replay behavior; strict identity/action validation is the same as L01. Evidence: `tests/smoke/app.spec.ts`, start/reset and local-attempt tests; `tests/contracts/replay.test.ts`; `tests/unit/storage-replays.test.ts`. |
| L03 | `IMPLEMENTED_PROTOTYPE` — same shared synthetic/non-navigation session boundary and L03 trace boundary copy. Evidence: `tests/smoke/app.spec.ts`, `reaches the L03 trace...`. | `IMPLEMENTED_PROTOTYPE` — manifest-only HUD projects status-tagged fields. `DECLARED_SYNTHETIC_ONLY` — gust/wave cue and sail/reef state. `NOT_IMPLEMENTED` — apparent wind and control indicator remain `declared_unavailable`; no physical gust/wave model is supplied. Evidence: `src/content/l02-l05.ts` (`l03Manifest`); `tests/smoke/app.spec.ts`, L03 observation and trace assertions. | `IMPLEMENTED_PROTOTYPE` — helm, main/jib trim, `reef`, pause/resume/reset only; disallowed controls are ignored/denied. Evidence: `src/content/l02-l05.ts`; `tests/contracts/lesson-manifest.test.ts`; `tests/smoke/app.spec.ts`. | `IMPLEMENTED_PROTOTYPE` — deterministic synthetic episode ledger event at tick 0 and immutable L03 reef checkpoint when `reef` is accepted. Evidence: `src/sim/session.ts` (`advanceLogicalTick`, `applyCanonicalInput`); `tests/unit/l02-l05.test.ts`; `tests/unit/projection.test.ts`, L03 trace tests. | `IMPLEMENTED_PROTOTYPE` — generic debrief and display-only episode/action/checkpoint trace read exact ledger records. `NOT_IMPLEMENTED` — real safety conclusion or reef-timing guidance. Evidence: `src/scoring/projection.ts` (`projectL03RuntimeTrace`); `tests/smoke/app.spec.ts`, L03 trace boundary assertion. | `IMPLEMENTED_PROTOTYPE` — shared reset/local attempt/replay behavior; strict identity/action validation is the same as L01. Evidence: `tests/smoke/app.spec.ts`; `tests/contracts/replay.test.ts`; `tests/unit/storage-replays.test.ts`. |
| L04 | `IMPLEMENTED_PROTOTYPE` — same shared synthetic/non-navigation session boundary and L04 runtime-evidence boundary copy. Evidence: `tests/smoke/app.spec.ts`, `renders uniquely named L04 runtime evidence...`. | `DECLARED_SYNTHETIC_ONLY` — heading, COG, STW, SOG and drift are now genuinely *computed*: L04 runs the polar-and-current model (`model_version: polar-kinematics-v5`), so SOG diverges from STW and COG from heading whenever the session-start-derived current is nonzero. The values rest on `VR-POLAR-v0`, disposition `assumption` — invented educational constants, not real navigational readings, tide data, or bearing guidance. The virtual mark relation remains a declared synthetic label. Evidence: `src/content/l02-l05.ts` (`l04Manifest`); `src/sim/polar-kinematics-model.ts`; `src/sim/polar-observation.ts`; `tests/unit/l04-current-correction.test.ts`, `separates SOG from STW and COG from heading under a declared cross-current`; `tests/smoke/app.spec.ts`, `shows computed STW, SOG, COG and drift for the L04 polar lesson, never the raw status token`. `NOT_IMPLEMENTED` — wave and visibility observations. | `IMPLEMENTED_PROTOTYPE` — helm, main/jib trim, `reef`, pause/resume/reset; decision controls and other disallowed controls do not apply. Trim and reef are consequential rather than declarative: a sheet moved off the declared optimum reduces the polar's base speed, and reefing scales it by the declared factor so the boat reaches the mark strictly later. `reef` lives here rather than on L03 because L03 terminates at reef selection, leaving a speed factor zero ticks to act. Evidence: `src/content/l02-l05.ts` (`L04_SEMANTIC_ACTIONS`); `src/sim/sail-trim.ts`; `tests/contracts/lesson-manifest.test.ts`; `tests/unit/l04-current-correction.test.ts`, `makes reefing slow the boat and delay the declared mark arrival`; `tests/unit/sail-trim.test.ts`. | `IMPLEMENTED_PROTOTYPE` — immutable L04 checkpoints explicitly record a recoverable synthetic mark miss and later slower valid synthetic correction. Evidence: `src/sim/session.ts`; `tests/unit/l02-l05.test.ts`; `tests/unit/projection.test.ts`, L04 checkpoint tests. | `IMPLEMENTED_PROTOTYPE` — generic debrief plus display-only L04 runtime evidence matched by exact checkpoint cause. The renderer draws the boat at its declared position rotated to its declared heading, uses immutable-ledger recorded positions as the synthetic trail, and draws the declared mark with its declared arrival radius. Canvas information is also supplied through synthetic/unvalidated text because the canvas is `aria-hidden`. `NOT_IMPLEMENTED` — navigational result, correctness judgment, or actual current correction. Evidence: `src/render/session-view.ts`; `src/render/world-projection.ts`; `tests/smoke/app.spec.ts`, `renders a moving synthetic track and a declared mark distance for L04`. | `IMPLEMENTED_PROTOTYPE` — shared reset/local attempt/replay behavior; strict identity/action validation is the same as L01. Evidence: `tests/smoke/app.spec.ts`; `tests/contracts/replay.test.ts`; `tests/unit/storage-replays.test.ts`. |
| L05 | `IMPLEMENTED_PROTOTYPE` — same shared synthetic/non-navigation session boundary, with an explicit record-visibility-only notice. Evidence: `tests/smoke/app.spec.ts`, `renders L05 decision-ledger record visibility...`. | `IMPLEMENTED_PROTOTYPE` — manifest-only HUD projects status-tagged fields. `DECLARED_SYNTHETIC_ONLY` — tide state and depth datum. `NOT_IMPLEMENTED` — scenario depth, clearance, visibility, and route state remain `declared_unavailable`; no tide/depth/visibility model is supplied. Evidence: `src/content/l02-l05.ts` (`l05Manifest`); `tests/smoke/app.spec.ts`, L05 observation assertion. | `IMPLEMENTED_PROTOTYPE` — helm, `decision_pass`, `decision_wait`, `decision_return`, pause/resume/reset only; trim actions and other disallowed controls deny. Evidence: `src/content/l02-l05.ts`; `tests/contracts/lesson-manifest.test.ts`; `tests/smoke/app.spec.ts`, `ignores globally mapped keyboard actions...`. | `IMPLEMENTED_PROTOTYPE` — each accepted declared decision appends its exact immutable L05 checkpoint record. Evidence: `src/sim/session.ts`; `tests/unit/l02-l05.test.ts`; `tests/unit/projection.test.ts`, L05 decision-ledger tests. | `IMPLEMENTED_PROTOTYPE` — the debrief exposes accepted-action and exact checkpoint record IDs only. `pass`, `wait`, and `return` are record labels only, never recommendations, correctness outcomes, or safety results. Evidence: `src/scoring/projection.ts` (`projectL05DecisionLedger`); `tests/smoke/app.spec.ts`, L05 record-visibility test. | `IMPLEMENTED_PROTOTYPE` — shared reset/local attempt/replay behavior; strict identity/action validation is the same as L01. Evidence: `tests/smoke/app.spec.ts`; `tests/contracts/replay.test.ts`; `tests/unit/storage-replays.test.ts`. |
| L06 | `IMPLEMENTED_PROTOTYPE` — a NEW, additive, non-mandatory lesson (the PRD §7.2 mandatory MVP set remains L01–L05); same shared synthetic/non-navigation session boundary text as every other lesson. Evidence: `src/content/l06-polar.ts` (`l06Manifest`); `tests/smoke/app.spec.ts`, `makes L06 selectable and runs it with the same non-navigation boundary text as other lessons`. | `DECLARED_SYNTHETIC_ONLY` — true/apparent wind, heading, COG, STW, SOG, drift, and current are all `declared_synthetic`. STW/SOG/COG/drift are genuinely *computed* (bilinear polar lookup composed with a derived current vector, per PRD §8.1 wind/current rows and FR-04) rather than static text, but the computation rests on `VR-POLAR-v0`, disposition `assumption`: 48 invented educational polar values with no domain validation, so the numbers remain synthetic, not real navigational readings. The current is now derived from the real date and time the player starts a session: `src/main.ts` reads `Date.now()` exactly once at session start, stores it as `current_epoch_ms` on the replay identity, and the pure `src/sim/tidal-current.ts` (`deriveSyntheticCurrent`) turns that stored timestamp into a current vector via a single sinusoid over a simplified 12.42-hour semidiurnal period — a declared educational assumption, not real tide data, harmonic constants, or a navigational current prediction. `src/sim` never reads the wall clock itself, so a replay of a stored identity reproduces exactly; only a fresh session started at a different real time gets a different current. Because the derivation is a function of the stored timestamp, SOG genuinely diverges from STW and COG from heading in the running app whenever that derived current is nonzero — this is exercised end to end, not just in a hypothetical unit-test override. The canonical fixture (`current_epoch_ms: 0`) still derives exactly zero current (`sin(0) === 0`), which keeps the pre-existing zero-current case meaningful. Evidence: `src/contracts/polar-profile.ts` (`trainingSloopPolarV1`); `src/sim/polar.ts`; `src/sim/tidal-current.ts`; `src/sim/polar-kinematics-model.ts`; `src/sim/polar-observation.ts`; `tests/unit/polar.test.ts`, `interpolates bilinearly between grid points`; `tests/unit/tidal-current.test.ts`; `tests/unit/polar-observation.test.ts`, `separates SOG from STW and COG from heading when a current is present...`, `reports sog === stw and cog === heading once the declared current is zero (FR-04, end to end)`; `tests/unit/l06-polar-lesson.test.ts`, `reports sog !== stw and cog !== heading for a session started at a real-world timestamp that derives a nonzero current (end to end, drift now observable)`; `tests/smoke/app.spec.ts`, `shows numeric STW and SOG for the L06 polar lesson after advancing, never the raw unavailable sentinel`. `NOT_IMPLEMENTED` — tide, wave, and visibility observations (PRD §8.1); trim/reef/wave/safety correction coefficients do not feed the polar, so L02/L03-style trim and reef inputs would change nothing for this model. | `IMPLEMENTED_PROTOTYPE` — `helm_port`, `helm_starboard`, `pause`, `resume`, `reset` only; no trim/reef/decision actions apply. Evidence: `src/content/l06-polar.ts` (`L06_SEMANTIC_ACTIONS`); `tests/contracts/lesson-manifest.test.ts`, `%s policy matrix admits every and only its declared actions` (parametrized over the full lesson registry, including L06); `tests/content/sailing-scenario-catalog.test.ts`, permitted-actions assertion for `L06`. | `IMPLEMENTED_PROTOTYPE` — every advance appends an immutable `POLAR_KINEMATIC_TRANSITION` ledger event carrying the polar transition detail and ordered causal helm controls; accepted helm additionally appends a `LESSON_CHECKPOINT` (`lesson_id: L06`, cause `declared helm correction recorded`). Evidence: `src/sim/session.ts` (`applyCanonicalInput`, `advanceLogicalTick`); `tests/unit/l06-polar-lesson.test.ts`, `appends a POLAR_KINEMATIC_TRANSITION ledger event on advance`, `accepts helm input on the L06 path and populates causal_controls in the resulting transition`. | `IMPLEMENTED_PROTOTYPE` — generic debrief projects one `synthetic_transition` fact per `POLAR_KINEMATIC_TRANSITION` event, each pointing at its causing ledger event; no course, safety, or correctness judgment is made, and `total_points` remains 0 (PRD §7.3 five-component scoring is not closed). The renderer draws the boat at its declared position rotated to its declared heading and uses immutable-ledger recorded positions as the synthetic trail; L06 declares no virtual mark. Canvas information is also supplied through synthetic/unvalidated text because the canvas is `aria-hidden`. Evidence: `src/render/session-view.ts`; `src/render/world-projection.ts`; `tests/unit/render-session-view.test.ts`; `src/scoring/projection.ts` (`projectDebrief`); `tests/unit/l06-polar-lesson.test.ts`, `projects one synthetic_transition debrief fact per polar transition, pointing at its ledger event`; `tests/unit/projection.test.ts`, `projects one synthetic_transition fact per POLAR_KINEMATIC_TRANSITION ledger event...`. | `IMPLEMENTED_PROTOTYPE` — shared reset/local-attempt/replay behavior; the replay identity carries and validates `polar_kinematics_environment`, selected by `model_version: 'polar-kinematics-v5'`, rejecting identities with a missing, substituted, or malformed environment. Every field is pinned to the canonical singleton by exact equality except `current_epoch_ms`, which legitimately varies per session and is checked structurally instead (a non-negative safe integer) — the one intentional relaxation of the replay identity contract. Determinism is exercised directly: two identical L06 sessions advanced the same number of ticks produce deep-equal raw state and ledger. Evidence: `src/contracts/replay.ts`; `src/sim/session.ts`; `src/main.ts` (L06 branch reading `Date.now()` once and freezing `polar_kinematics_environment` on start/reset); `tests/contracts/replay.test.ts`, `describe('polar kinematics replay identity carrier')`; `tests/unit/l06-polar-lesson.test.ts`, `produces deep-equal raw state and ledger for two identical L06 sessions advanced the same number of ticks`. |

## Limits

This matrix does **not** establish real sailing physics, safety, navigation authority, certification, supported-browser coverage, domain validation, or release eligibility. All lesson validation dispositions remain `assumption`; the matrix does not promote the registry or change release gating. It also does not convert L05 record labels into recommendations, correctness outcomes, or safety results.

The polar work does not change this posture. `VR-POLAR-v0` is `disposition: assumption` (`docs/content/domain-validation-registry.yaml`); its 48 polar values, the trim and reef coefficients, and the tide, clearance, seabed-depth and draft constants are all invented educational assumptions asserting no real hull performance, tide, depth, or safety behaviour. L06 remains outside the PRD §7.2 mandatory MVP set (L01–L05).

**Which lessons run which model.** The polar model was first added additively as L06, because migrating **L01** would have silently reinterpreted its replay bindings at 78 call sites across 5 test files. **L04 was subsequently migrated in place** — its bindings have 3 usages — so L04 now runs the polar-and-current model, derives mark arrival from real position, and had its two golden fixtures regenerated. **L01, L02, L03 and L05 remain byte-identical** and still run the legacy fixed-speed model with their observations declared unavailable; their golden fixtures were not regenerated.

**Tide, clearance and threshold events** apply to every polar session (L04 and L06): a synthetic semidiurnal tide drives `seabed depth + tide - draft`, and crossing a declared clearance threshold records exactly one event per severity level (PRD FR-05). The stream is the tide's rate of change on one shared phase, both advancing with logical time from the single stored session-start timestamp. None of it is real tide data, harmonic constants, charted depth, soundings, a datum, or an under-keel safety margin.

Tracked gaps:
- **State-contract inconsistency.** `heading` / `cog` / `true_wind` / `apparent_wind` use the string sentinel `'declared-unavailable'` when absent, while `stw` / `sog` / `drift_angle` are optional fields that are `undefined` on non-polar lessons — a consumer must handle three states, not two. Resolving this fully would require regenerating the L02–L05 fixtures, which is out of scope here.
- **Defensive-branch coverage.** The HUD's handling of the `'declared-unavailable'` and `undefined` cases for STW/SOG/drift is not exercised by tests, because a live polar session always produces numeric values; TypeScript `strict` mode, not a test, is what currently prevents a naive two-state rewrite from compiling.
